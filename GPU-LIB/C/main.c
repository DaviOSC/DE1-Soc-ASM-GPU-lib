#include <stdio.h>
#include <fcntl.h>
#include <sys/mman.h>
#include <sys/stat.h>
#include "fpga_gpu.h"

int main()
{
    // create_mapping_memory();
    // set_background_color(0, 7, 0);
    // set_background_block(10, 10, 7, 0, 0);
    // set_polygon(0, 0, 0, 1, 100, 100);
    // set_sprite(0, 0, 1, 200, 200);
    // close_mapping_memory();
    return 0;
}